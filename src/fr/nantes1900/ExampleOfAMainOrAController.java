package fr.nantes1900;

public class ExampleOfAMainOrAController
{
    public static void main(String[] args)
    {
        //
        // String directoryName = "";
        //
        // Town town = new Town(directoryName);
        //
        // while (town.hasMoreResidentials())
        // {
        // Mesh res = town.nextResidential();
        // // Extract the normal gravity oriented and change it to the new
        // // base.
        // final Vector3d normalGround = Town
        // .extractGroundNormal(directoryName
        // + FilesNames.GRAVITY_GROUND_FILENAME
        // + FilesNames.EXTENSION);
        // town.createChangeBaseMatrix(normalGround);
        // Mesh ground = ExampleOfAMainOrAController.chooseGround(res);
        // res.changeBase(MatrixMethod.createOrthoBase(ground.averageNormal()));
        // Mesh groundReal = Town
        // .groundExtraction(res, ground.averageNormal());
        // List<Building> buildings = Town.buildingsExtraction(mesh, noise);
        //
        // // Treat every kind of surfaces put in the algorithms.
        // town.treatGrounds(directoryName + FilesNames.GROUNDS_NAME);
        //
        // town.treatWateries(directoryName + FilesNames.WATERIES_NAME);
        //
        // town.treatSpecialBuildings(directoryName
        // + FilesNames.SPECIAL_BUILDINGS_NAME);
        //
        // town.treatIndustrials(directoryName + FilesNames.INDUSTRIALS_NAME,
        // normalGround);
        //
        // town.treatResidentials(
        // directoryName + FilesNames.RESIDENTIALS_NAME, normalGround);
        //
        // }
    }
}
